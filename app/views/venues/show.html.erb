<h1> <%= @venue.name %> </h1>

<p><%= @venue.description %></p>

<h3>Next Show:</h3>
<% if @next_show.nil? %>
  <p>There are currently no shows scheduled at this venue.</p>
<% else %>
  <p> <%= format_showtime(@next_show.showtime) %> - <%=@next_show.band.name %></p>

  <h3> <%= link_to "See all upcoming shows", venue_bookings_path(@venue) %></h3>
<% end %>
<h4><%= link_to "Book a band at this venue", new_venue_booking_path(@venue) %></h4>

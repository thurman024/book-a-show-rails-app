<h1>Venue Index</h1>

<ul>
  <% @venues.each do |venue| %>
    <li> <%= link_to venue.name, venue_path(venue) %> - <%= show_count_message(venue.bookings) %></li><br>
  <% end %>
</ul>

<br>
<p><%= link_to "Add a Venue", new_venue_path %></p>

<h1>All Shows</h1>

<% @bookings.each do |show| %>
  <p><%= link_to show.band.name, band_path(show.band) %> at <%= show.venue.name %> </p>
  <p> <%= show.showtime.strftime("%B %-d, %Y at %-I:%M%p") %></p><br><br>
<% end %>
